--- CssParser.java
+++ CssParser.java
@@ -6063,7 +6063,14 @@ n.image = Util.strip(n.image);
 	 }
 	 token = oldToken;
 	 jj_kind = kind;
-	 throw generateParseException();
+	 if (kind == LBRACE) {
+	   if (ac.getTreatVendorExtensionsAsWarnings()) {
+		ac.getFrame().addWarning("vendor-extension", skipStatement());
+	   } else {
+		addError(generateParseException(), skipStatement());
+	   }
+	 }
+	 return token;
   }
 
   @SuppressWarnings("serial")
